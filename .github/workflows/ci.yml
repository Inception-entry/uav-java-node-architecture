name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend-java:
    name: Java backend
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend-java
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Test and package
        run: mvn --batch-mode --no-transfer-progress verify

  gateway-java:
    name: Spring Cloud Gateway
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: gateway-java
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Test and package
        run: mvn --batch-mode --no-transfer-progress verify

  backend-ai:
    name: AI service (Python)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: backend-ai
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv
        run: pip install uv --quiet
      - name: Install dependencies
        run: uv sync --group dev
      - name: Run tests
        run: uv run pytest tests/ -v

  backend-node:
    name: Node BFF
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: backend-node
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Node.js 20
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: backend-node/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Audit dependencies
        run: npm audit --audit-level=high --omit=dev
      - name: Build and test JWT authorization
        run: npm test

  frontend:
    name: Vue frontend
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Node.js 22
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Audit dependencies
        run: npm audit --audit-level=high --omit=dev
      - name: Type-check and build
        run: npm run build
      - name: Verify critical API path contracts
        run: npm test

  admin-service:
    name: Admin service (NestJS)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: admin-service
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Node.js 20
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: admin-service/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Generate Prisma client
        run: npx prisma generate
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
      - name: Audit dependencies
        run: npm audit --audit-level=high --omit=dev

  admin-frontend:
    name: Admin frontend (React)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: admin-frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Node.js 22
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: admin-frontend/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Audit dependencies
        run: npm audit --audit-level=high --omit=dev
      - name: Type-check and build
        run: npm run build

  docker-e2e:
    name: Docker full-stack integration
    needs:
      - backend-ai
      - backend-java
      - gateway-java
      - backend-node
      - frontend
      - admin-service
      - admin-frontend
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Prepare isolated CI environment
        shell: bash
        run: |
          set -euo pipefail
          ci_env="${RUNNER_TEMP}/skytrace-ci.env"
          cp deploy/.env.example "$ci_env"

          replace_env() {
            local key="$1"
            local value="$2"
            sed -i "s|^${key}=.*|${key}=${value}|" "$ci_env"
          }

          mysql_root_password="ci-root-$(openssl rand -hex 16)"
          mysql_password="ci-mysql-$(openssl rand -hex 16)"
          rabbitmq_password="ci-rabbit-$(openssl rand -hex 16)"
          minio_password="ci-minio-$(openssl rand -hex 16)"
          keycloak_admin_password="ci-admin-$(openssl rand -hex 16)"
          service_secret="$(openssl rand -hex 32)"
          test_user_password="ci-user-$(openssl rand -hex 16)"
          admin_jwt_secret="ci-admin-jwt-$(openssl rand -hex 24)"
          admin_jwt_refresh="ci-admin-refresh-$(openssl rand -hex 24)"
          postgres_password="ci-pg-$(openssl rand -hex 12)"

          replace_env MYSQL_ROOT_PASSWORD "$mysql_root_password"
          replace_env MYSQL_PASSWORD "$mysql_password"
          replace_env RABBITMQ_DEFAULT_PASS "$rabbitmq_password"
          replace_env MINIO_ROOT_PASSWORD "$minio_password"
          replace_env KEYCLOAK_ADMIN_PASSWORD "$keycloak_admin_password"
          replace_env KEYCLOAK_SERVICE_CLIENT_SECRET "$service_secret"
          replace_env KEYCLOAK_DEV_USER_PASSWORD "$test_user_password"
          replace_env ADMIN_JWT_SECRET "$admin_jwt_secret"
          replace_env ADMIN_JWT_REFRESH_SECRET "$admin_jwt_refresh"
          replace_env POSTGRES_PASSWORD "$postgres_password"
          # Playwright / curl 走 127.0.0.1；issuer 必须与 KC_HOSTNAME 一致，否则 Gateway 验签 401
          replace_env KEYCLOAK_PUBLIC_URL "http://127.0.0.1:8180"
          replace_env GATEWAY_JWT_ISSUER_URI "http://127.0.0.1:8180/realms/skytrace"
          replace_env GATEWAY_ALLOWED_ORIGIN "http://127.0.0.1:8888"

          echo "::add-mask::$mysql_root_password"
          echo "::add-mask::$mysql_password"
          echo "::add-mask::$rabbitmq_password"
          echo "::add-mask::$minio_password"
          echo "::add-mask::$keycloak_admin_password"
          echo "::add-mask::$service_secret"
          echo "::add-mask::$test_user_password"
          echo "::add-mask::$admin_jwt_secret"
          echo "::add-mask::$admin_jwt_refresh"
          echo "::add-mask::$postgres_password"
          echo "SKYTRACE_ENV_FILE=$ci_env" >> "$GITHUB_ENV"
          echo "KEYCLOAK_CLIENT_SECRET=$service_secret" >> "$GITHUB_ENV"
          echo "KEYCLOAK_DEV_USER_PASSWORD=$test_user_password" >> "$GITHUB_ENV"
          echo "COMPOSE_PROJECT_NAME=skytrace-ci" >> "$GITHUB_ENV"

      - name: Start Ollama health-check mock
        shell: bash
        run: |
          OLLAMA_MOCK_HOST=0.0.0.0 \
          nohup python3 scripts/ci/ollama-mock.py \
            > "${RUNNER_TEMP}/ollama-mock.log" 2>&1 &
          echo "$!" > "${RUNNER_TEMP}/ollama-mock.pid"
          scripts/ci/wait-for-http.sh \
            http://127.0.0.1:11434/api/tags 30 "Ollama mock"

      - name: Validate Compose configuration
        run: |
          docker compose \
            --env-file "$SKYTRACE_ENV_FILE" \
            -f deploy/docker-compose.yml \
            config --quiet

      - name: Build application images
        run: |
          docker compose \
            --env-file "$SKYTRACE_ENV_FILE" \
            -f deploy/docker-compose.yml \
            build backend-ai backend-java backend-node gateway frontend admin-service admin-frontend

      - name: Scan application images for vulnerabilities
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh \
            | sh -s -- -b /usr/local/bin latest
          for svc in backend-ai backend-java backend-node gateway frontend admin-service admin-frontend; do
            image="${COMPOSE_PROJECT_NAME}-${svc}"
            docker image inspect "$image" >/dev/null
            echo "=== Trivy: ${svc} ==="
            trivy image \
              --scanners vuln \
              --severity HIGH,CRITICAL \
              --exit-code 1 \
              --ignore-unfixed \
              "$image"
          done

      - name: Start backend integration stack
        run: |
          docker compose \
            --env-file "$SKYTRACE_ENV_FILE" \
            -f deploy/docker-compose.yml \
            up -d --no-build gateway frontend

      - name: Wait for application readiness
        shell: bash
        run: |
          scripts/ci/wait-for-http.sh \
            http://127.0.0.1:8180/realms/skytrace/.well-known/openid-configuration \
            240 Keycloak
          scripts/ci/wait-for-http.sh \
            http://127.0.0.1:8000/health 180 "AI service"
          scripts/ci/wait-for-http.sh \
            http://127.0.0.1:8081/api/health 240 "Java backend"
          scripts/ci/wait-for-http.sh \
            http://127.0.0.1:3000/api/health 120 "Node BFF"
          scripts/ci/wait-for-http.sh \
            http://127.0.0.1:8082/actuator/health 120 Gateway
          scripts/ci/wait-for-http.sh \
            http://127.0.0.1:8888/ 120 Frontend

      - name: Verify three-role authorization
        run: |
          ./scripts/skytrace.sh auth-users
          ./scripts/skytrace.sh auth-verify

      - name: Verify Temporal and MySQL full stack
        env:
          CI_TASK_CODE: CI-${{ github.run_id }}-${{ github.run_attempt }}
        run: scripts/ci/verify-stack.sh

      - name: Verify alarm and evidence pipeline
        env:
          CI_ALARM_TASK_CODE: ALARM-${{ github.run_id }}-${{ github.run_attempt }}
        run: scripts/ci/verify-alarm-evidence.sh

      - name: Setup Node.js for Playwright
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Playwright and run browser E2E
        working-directory: e2e
        env:
          E2E_BASE_URL: http://127.0.0.1:8888
          KEYCLOAK_DEV_USER_PASSWORD: ${{ env.KEYCLOAK_DEV_USER_PASSWORD }}
          KEYCLOAK_CLIENT_SECRET: ${{ env.KEYCLOAK_CLIENT_SECRET }}
          KEYCLOAK_URL: http://127.0.0.1:8180
          GATEWAY_URL: http://127.0.0.1:8082
        run: |
          npm install
          npx playwright install --with-deps chromium
          npm test

      - name: Collect failure diagnostics
        if: failure()
        shell: bash
        run: |
          set +e
          log_dir="${RUNNER_TEMP}/skytrace-ci-logs"
          mkdir -p "$log_dir"
          docker compose \
            --env-file "$SKYTRACE_ENV_FILE" \
            -f deploy/docker-compose.yml \
            ps --all > "$log_dir/compose-ps.txt" 2>&1
          docker compose \
            --env-file "$SKYTRACE_ENV_FILE" \
            -f deploy/docker-compose.yml \
            logs --no-color --timestamps > "$log_dir/compose.log" 2>&1
          docker images > "$log_dir/docker-images.txt" 2>&1
          cp "${RUNNER_TEMP}/ollama-mock.log" \
            "$log_dir/ollama-mock.log" 2>/dev/null || true

      - name: Upload failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: docker-e2e-logs-${{ github.run_id }}
          path: ${{ runner.temp }}/skytrace-ci-logs
          if-no-files-found: warn
          retention-days: 7

      - name: Stop integration stack
        if: always()
        shell: bash
        run: |
          if [[ -n "${SKYTRACE_ENV_FILE:-}" && -f "$SKYTRACE_ENV_FILE" ]]; then
            docker compose \
              --env-file "$SKYTRACE_ENV_FILE" \
              -f deploy/docker-compose.yml \
              down --volumes --remove-orphans
          fi
          if [[ -f "${RUNNER_TEMP}/ollama-mock.pid" ]]; then
            kill "$(cat "${RUNNER_TEMP}/ollama-mock.pid")" \
              2>/dev/null || true
          fi
